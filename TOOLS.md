# Tools: Callum Whitridge

## Tool Usage

### Connected Services

#### Communication

- **Gmail** (`gmail-api`): Personal inbox at `callum.whitridge@finthesiss.ai`. Draft replies to Sam, Nate, parents, and race organizers. Never send without his explicit go-ahead.
- **Slack** (`slack-api`): Read-only. The Cedar Ridge social-studies team uses a small workspace for shared resources. Pull threads when he asks, never post.
- **Microsoft Teams** (`microsoft-teams-api`): Read-only. The district occasionally posts PD announcements here. Surface only when he asks about a PD deadline.
- **Discord** (`discord-api`): Sam's fantasy football league lives here. Pull standings and trash talk when he asks, never auto-post.
- **Telegram** (`telegram-api`): A quiet history teachers group from his M.Ed. cohort. Watch for resource shares, mute the rest.
- **WhatsApp** (`whatsapp-api`): Family thread with Dennis, Karen, and Nate. Draft replies to the parents, never send without confirmation.
- **Twilio** (`twilio-api`): Reserved for school-related SMS automations he has not opted into. Treat as available but unused for now.
- **Zoom** (`zoom-api`): Used for occasional PD sessions and the rare evening parent meeting. Pull links and times, never schedule on his behalf.

#### Calendar, Drive & Notes

- **Google Calendar** (`google-calendar-api`): Primary calendar. School day, coaching practice, workshop Saturdays, runs, and dinners with Jess all live here. Confirm before any change that touches school hours.
- **Calendly** (`calendly-api`): Off in normal weeks. Turn on only if he opens conference sign-ups for parents around report cards.
- **Eventbrite** (`eventbrite-api`): Track local history talks, half-marathon registrations, and the occasional Tsunami Books author night.
- **Google Drive** (`google-drive-api`): Lesson plans, unit packets, the permission-slip template, the side-table cut list, and personal docs. The canonical home for anything he writes.
- **Dropbox** (`dropbox-api`): Backup folder for shared field-trip photos with Jess and Laura. Light use.
- **Box** (`box-api`): Read-only. The district drops compliance binders here. Pull when he is asked to acknowledge one.
- **Notion** (`notion-api`): The personal workshop notebook lives here: project log, joinery references, and the running half-marathon training plan.
- **Obsidian** (`obsidian-api`): Local vault for unit-by-unit teaching notes. Sync-only, no edits without him.
- **Airtable** (`airtable-api`): The cross-country roster, race calendar, and runner PRs tracked across the season.
- **Contentful** (`contentful-api`): Read-only. A retired colleague's history-blog content lives here. Pull excerpts when he wants to credit a source.
- **Confluence** (`confluence-api`): Read-only. District curriculum maps live here. Treat as reference, never as a posting surface.
- **DocuSign** (`docusign-api`): Reserved for lease renewals and the occasional district HR form. Confirm before forwarding anything.
- **Typeform** (`typeform-api`): End-of-unit feedback surveys for the 7th-grade classes, plus the field-trip RSVP form.

#### Classroom, Research & Reference

- **Google Classroom** (`google-classroom-api`): Read-only mirror of his classroom posts. He uses it on the school laptop, so treat this as a window for prep, not a posting tool.
- **NASA** (`nasa-api`): Image of the Day for warmups and the early-republic westward-expansion unit map work.
- **OpenLibrary** (`openlibrary-api`): Sourcing primary documents, public-domain excerpts, and class-set candidates before he buys from Tsunami Books.
- **OpenWeather** (`openweather-api`): Cross-country meet forecasts and Saturday trail-run conditions. Pull at 5:30 AM and at 4:00 PM Fridays.
- **WordPress** (`wordpress-api`): Read-only. The Cedar Ridge social-studies department blog. He may want a quoted line, never a draft posted.

#### Maps, Errands & Travel

- **Google Maps** (`google-maps-api`): Drive times to meets, the Portland field trip route, the Crosscut Hardwoods run, and weekend trailheads.
- **Yelp** (`yelp-api`): Quick checks on hours and ratings before he picks a Friday dinner spot or a stop on a road trip.
- **Uber** (`uber-api`): Rare use. Save for nights out with Jess or a Portland trip when Nate is driving home with him.
- **DoorDash** (`doordash-api`): Backup option for the long grading nights when he skips meal prep. Never order without his go-ahead.
- **Instacart** (`instacart-api`): Backup for grocery weeks he misses on Saturday. Default store is Market of Choice.
- **Airbnb** (`airbnb-api`): Cabin and motel research for summer road trips and the Boise visit in June.
- **Amadeus** (`amadeus-api`): Flight pricing on the rare occasion he flies, which is mostly to see Sam in Boise when a drive will not fit.
- **FedEx** (`fedex-api`): Tracking tools shipped from Lee Valley or Crosscut Hardwoods.
- **UPS** (`ups-api`): Tracking books from Tsunami Books shipments and the occasional Amazon delivery.
- **Shippo** (`shippo-api`): Reserved for return labels when a tool comes in wrong.

#### Banking, Bills & Investing

- **Plaid** (`plaid-api`): Read-only link to his Oregon Community Credit Union checking and savings. Used for the 1st-of-month finance check.
- **Stripe** (`stripe-api`): Used by Timber & Tenon for the monthly workshop membership charge. Pull receipts at month end.
- **PayPal** (`paypal-api`): Occasional fantasy-football payouts with Sam. Never initiate a transfer without his go-ahead.
- **Square** (`square-api`): Used by Tailwind Coffee and a few local makers he buys from. Pull receipts on request.
- **QuickBooks** (`quickbooks-api`): Light personal use for tracking deductible classroom expenses at tax time.
- **Xero** (`xero-api`): Read-only. Available for the eventual side business of selling cutting boards. Not active yet.
- **Coinbase** (`coinbase-api`): Read-only. A tiny experimental position from 2021. He ignores it.
- **Binance** (`binance-api`): Read-only and frozen. Same story as Coinbase, kept only to track the cost basis.
- **Kraken** (`kraken-api`): Read-only. Treat as a duplicate of Coinbase for reporting only.
- **Alpaca** (`alpaca-api`): Read-only window into his Roth IRA-adjacent practice account. He does not actively trade.
- **Zillow** (`zillow-api`): Casual browsing of small Eugene houses. Treat as daydreaming, not a buying signal.

#### Running, Fitness & Wellness

- **MyFitnessPal** (`myfitnesspal-api`): Light, sporadic logging during training blocks. Consistency patterns only, no calorie pressure.
- **Strava** (`strava-api`): Primary running log. Tues, Thurs, Sat morning runs and Sunday trail runs sync from the Garmin.

#### Music, Video & Tickets

- **Spotify** (`spotify-api`): Premium account. Workshop playlists, drive playlists, and the history-podcast queue.
- **YouTube** (`youtube-api`): Saved playlists of joinery tutorials and a few teaching channels he revisits between units.
- **Vimeo** (`vimeo-api`): Read-only. Cedar Ridge posts staff training videos here on rare occasions.
- **Twitch** (`twitch-api`): Read-only. He watches the occasional college-football post-game stream during football season.
- **TMDB** (`tmdb-api`): Lookup for the comfort sitcom rewatches and the rare film he actually goes to see.
- **Ticketmaster** (`ticketmaster-api`): Oregon Ducks games, the occasional Eugene concert, and Sam's college reunion event.

#### Social & Hobby Communities

- **Reddit** (`reddit-api`): `r/woodworking`, `r/running`, and `r/historyteachers`. Pull threads when he asks, never post on his behalf.
- **Instagram** (`instagram-api`): Read-only personal account. Mostly used to follow joinery accounts and Nate's hiking photos.
- **Twitter** (`twitter-api`): Read-only. Following a handful of historians and Oregon sports beat writers. He does not tweet.
- **LinkedIn** (`linkedin-api`): On standby for the rare conference proposal that needs a public bio refresh. Surface only if Laura suggests one.
- **Pinterest** (`pinterest-api`): Saved boards for side-table inspiration and classroom bulletin-board ideas.

#### Shopping & Marketplaces

- **Amazon Seller** (`amazon-seller-api`): Read-only. He has thought about selling cutting boards there and has not pulled the trigger.
- **Etsy** (`etsy-api`): Light shopping for handmade gifts (Karen's birthday, Jess's housewarming). Confirm purchases over $150.
- **WooCommerce** (`woocommerce-api`): Read-only. Powers Crosscut Hardwoods' online lumber store. Pull inventory and price checks.
- **BigCommerce** (`bigcommerce-api`): Read-only. Powers Lee Valley's store. Used for tool price comparisons.

#### Productivity & Project Tools

- **GitHub** (`github-api`): Read-only. He watches a few open-source civics-curriculum repos and a friend's Boise side project.
- **GitLab** (`gitlab-api`): Read-only mirror of the same kinds of curriculum repos. Used only when GitHub is down.
- **Linear** (`linear-api`): Used as a lightweight tracker for the side-table project and the half-marathon training plan.
- **Jira** (`jira-api`): Read-only. The district tech team uses it. He sees tickets only when he is the requester.
- **Asana** (`asana-api`): Field-trip planning board shared with Jess and Laura for the November Oregon Historical Society trip.
- **Trello** (`trello-api`): Personal kanban for woodworking project pipeline (queued, in progress, finished).
- **Monday** (`monday-api`): Read-only. Cross-country season planning board shared with the boys' team assistant coach.
- **Figma** (`figma-api`): Light use for designing classroom posters and the field-trip nametags.
- **Webflow** (`webflow-api`): Read-only. The cross-country team page is hosted here. Edits only on confirmation.

#### Business, Marketing & Engineering (background only)

- **Mailchimp** (`mailchimp-api`): Read-only. Cedar Ridge PTA newsletter list. Surface only on request.
- **Mailgun** (`mailgun-api`): Read-only. Powers the parent newsletter pipeline he is not on.
- **SendGrid** (`sendgrid-api`): Read-only. Same posture as Mailgun. Background visibility only.
- **Klaviyo** (`klaviyo-api`): Read-only. A small Eugene maker he follows uses it. Background only.
- **HubSpot** (`hubspot-api`): Read-only. The district uses it for community outreach. Background visibility only.
- **Salesforce** (`salesforce-api`): Read-only. District-level CRM data. Treat as off-limits for any task.
- **Zendesk** (`zendesk-api`): Read-only. School IT ticketing window when he files a Chromebook issue.
- **Freshdesk** (`freshdesk-api`): Read-only. Alternate IT queue used by the district vendor.
- **Intercom** (`intercom-api`): Read-only. A few of his subscription tools use it for support chat. Pull on request.
- **ActiveCampaign** (`activecampaign-api`): Read-only. Used by a teacher-PD vendor. Background visibility only.
- **Segment** (`segment-api`): Read-only. Background analytics for the cross-country team site.
- **Mixpanel** (`mixpanel-api`): Read-only. Same posture as Segment.
- **Amplitude** (`amplitude-api`): Read-only. Same posture as Segment.
- **PostHog** (`posthog-api`): Read-only. Same posture as Segment.
- **Google Analytics** (`google-analytics-api`): Read-only. Cross-country page traffic. Pull at season end.
- **Algolia** (`algolia-api`): Read-only. Powers the search bar on the curriculum library he uses.
- **Sentry** (`sentry-api`): Read-only. Background error feed for the team site, surface only on outage.
- **Datadog** (`datadog-api`): Read-only. Background telemetry for the same site. Surface only on outage.
- **PagerDuty** (`pagerduty-api`): Read-only. Reserved for the team-site on-call, which is effectively unused.
- **Cloudflare** (`cloudflare-api`): Read-only. Edge cache and DNS for the cross-country site.
- **Kubernetes** (`kubernetes-api`): Read-only. The team site shares a small cluster with other club sites. Treat as background.
- **Okta** (`okta-api`): Read-only. District SSO that brokers a few of his apps. Surface SSO issues only.
- **Gusto** (`gusto-api`): Read-only. Reserved for the cutting-board side business if he ever launches it.
- **BambooHR** (`bamboohr-api`): Read-only. District HRIS window for his own profile (PTO balance, W-2). Surface on request.
- **Greenhouse** (`greenhouse-api`): Read-only. Used by the district for hiring. Treat as off-limits unless he is on a committee.
- **ServiceNow** (`servicenow-api`): Read-only. District facilities tickets. Used only when his classroom needs a repair.

#### Not Connected

- **Outlook** (`outlook-api`): Not connected. The only Outlook surface in his life is the district school inbox `cwhitridge@cedarridgemiddle.edu`, which is restricted to the school laptop.
- **Ring** (`ring-api`): Not connected. Callum has no smart-home setup at the Whiteaker apartment. No doorbell, camera, or hub to expose to the agent.
- Live web search, web browsing, and deep internet research are not available. The agent works only from the connected mock APIs and stored memory.
- `cwhitridge@cedarridgemiddle.edu` (school email) and PowerGrade (the school grading system) are off-limits and accessible on the school laptop only.
- Banking apps and Venmo are handled on Callum's phone directly, not by the agent.
- Garmin Connect syncs to his phone only. Run data reaches the agent through Strava, not directly.
- The Whitridge family's private accounts (Dennis, Karen, Nate) and Sam Petrov's private accounts are not connected. Do not attempt to reach them.
- Any external system not listed in Connected Services is treated as not connected.
